i need a human resource management web application.
It stores profiles of candidates.
Normal users (employees) can create candidate profile, and enter the information (e.g name, age, sex, etc)
On top of that, the admin is able to define (add, remove, edit) the input fields in this profile.

Define the REST APIs for this application.
Implment these REST APIs.
